# 如何调试源码

用 VSCode + Bun 直接对 `src/` 源码打断点调试，不走构建产物（dist 是 minify 且无 sourcemap 的）。

## 环境准备

1. 安装 [Bun](https://bun.sh)
2. VSCode 安装 **Bun 插件**（`oven.bun-vscode`）
3. 仓库根目录执行一次初始化：

```bash
bun install
bun run build                 # 生成 node_modules 里的缺失包 stub，调试依赖它
bun devkit/debug-stubs.ts     # 生成 src 内被裁剪模块的 stub
```

`.vscode/launch.json` 与 `.vscode/tasks.json` 已在仓库里，开箱即用。

**免登录**：`devkit/debug-preload.ts` 启动时会自动读取 `~/.claude/settings.json` 的 `env` 块并注入进程（第三方 API 配置如智谱的 `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN` 等）。之所以要这步：源码调试时全局 settings.json 常因 schema 校验失败被整体跳过，官方的"settings env 自动生效"机制失效。只要你的 settings.json 里配好了第三方 API，两种调试方式都免登录；改 settings.json 后无需同步任何文件。

## 方式一：F5 直接启动（非交互）

1. 在 `src/**` 里打断点
2. 调试面板选 **「调试源码 CLI (Bun)」**，按需改配置里的 `args`：
   - `["--help"]`（默认）：只跑启动链路
   - `["-p", "你好"]`：完整跑一次非交互问答
3. F5 启动

进程执行完 `args` 就退出，属正常。不要往 `runtimeArgs` 里加 `--inspect-wait`。

## 方式二：终端启动 + Attach（交互式 TUI）

1. VSCode 集成终端运行：

```bash
./devkit/debug-tui.sh
```

（等价于 `bun --inspect-wait=ws://localhost:6499/ --preload ./devkit/debug-preload.ts src/entrypoints/cli.tsx`）

进程打印 `Listening: ws://localhost:6499/` 后停住等调试器，是预期行为。

2. 调试面板**下拉框切到「附加到 Bun (--inspect-wait)」**，按 F5
3. TUI 开始运行，断点正常生效

注意：`--inspect-wait` 不带 `=ws://...` 时每次会生成随机 token URL，就得把终端打印的完整 URL 粘进 launch.json；显式指定地址则 URL 固定。

## 常见情况

- **报 `Cannot find module './xxx.js'`**：重跑 `bun devkit/debug-stubs.ts`（或任务「devkit: 重新生成 debug stubs」）
- **启动弹 "Settings Error" 对话框**：你的配置文件里有本版本不认的字段（如 `permissions.defaultMode: "auto"`），选 "Continue without these settings" 继续即可，不用改真实的 settings.json
- **端口 6499 被占用**：终端命令和 launch.json 的 Attach `url` 同步换端口
- **断点不生效**：打开该文件看开头是否有 `__devkit_debug_stub__` 标记，stub 文件里没有真实逻辑
